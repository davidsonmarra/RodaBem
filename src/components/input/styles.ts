import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: '100%',
    height: 50,
    borderRadius: 10,
    paddingHorizontal: 10,
    borderWidth: 1,
  },
});
