import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: 'bold',
  },
  text: {
    fontSize: 16,
    fontWeight: 'normal',
  },
  textBold: {
    fontSize: 16,
    fontWeight: 'bold',
  },
});

export default styles;
